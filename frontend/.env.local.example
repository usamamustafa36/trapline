# Base URL of the central FastAPI ingestion/query API.
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
# Server-side (SSR/route handlers) can use an internal address inside Docker.
API_INTERNAL_BASE=http://localhost:8000/api/v1
